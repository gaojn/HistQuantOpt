# HistQuantOpt 项目审查报告（2026-07-16 · Fable5 综合）

- **审查对象**：HistQuantOpt @ 分支 `opt0630`（A股多因子组合优化 / 回测 / 归因）
- **审查方式**：4 路并行专项审查（优化器核心、回测引擎、前视与数据口径专项、工程与文档一致性），主会话交叉核对后综合
- **测试基线**：`python3 -m pytest tests/ -q` → **51/51 通过**，1.76s，无告警
- **前视专项结论**：**PASS**（活跃优化/回测/归因路径无实质前视）
- **红线核查结论**：**FAIL 1 处**（`trade_status == "交易"`，见 P0-1）

---

## 一、总体结论

核心链路时序干净、回测真实性设计到位，可用于研究生产；但 **1 处房规红线未修**，另有 **2 个系统性影响回测保真度的口径问题**（停牌资金口径、掉池持仓漂移），且**今晨审查记录的 8 项遗留待办全部未动**（期间提交均为新功能，无修复动作）。

复核确认做得好的地方：

- **无前视**：调仓日 T 的 alpha、CNE6 暴露/协方差、基准权重、ADV、成本向量、universe/ST/成分标记全部 as-of `<= T` 取数；权重记在 T、由引擎 T+1 VWAP 成交。三处 `shift(-n)` 全部位于显式标注"含未来信息"的合成信号工具（`signal_grid.py`、`build_alphas_vwap5.py`、`build_synthetic_alpha`），默认配置一律 `alpha.source: file`，不经过该路径。
- **回测引擎口径正确**：可交易判定写的是 `== "停牌"`（XD/XR/N 不误拦）；T+1 成交日独立判涨跌停；非对称费率、停牌卖单顺延、退市 ffill 清算均已实现。
- **归因模块（commit 2ac2165）测试质量良好**：Barra 恒等式自检、手算数值对齐、Carino 多期链接恒等式、覆盖缺口→残差检验。
- 无硬编码密钥；文档与 YAML 默认值抽查基本一致（8 项抽查 7 项一致）。

---

## 二、缺陷清单（统一定级）

### P0 —— 房规红线，立即修

**1. [real_adapter.py:275,283](portfolio_optimizer/data/real_adapter.py:275) 涨跌停判定用 `ts == "交易"` 前置**（= 今晨待办1，未修复）

`_compute_status` 中 hit_up / hit_down 均以 `(ts == "交易")` 为前置。XD/XR/N/DR 日均可正常交易且可能触及涨跌停，被漏判为 NORMAL：XD 日封涨停的持仓股本应标 `LIMIT_UP`（优化器约束 `w ≤ w_prev` 禁止加仓），实际却允许优化器自由加仓。

- **性质澄清**：这是可交易性口径红线，**不是前视**（T 日数据做 T 决策）。且 [engine.py:217](portfolio_optimizer/backtest/engine.py:217)-219 在 T+1 成交时独立按价格判涨跌停（无 `=="交易"` 前置），会拒绝不可执行的成交，净值层面有兜底——真实损失是优化目标含不可成交意图 → 现金拖累、换手意图虚高、跟踪噪声。
- **修复**：前置条件改 `ts != "停牌"`（与同函数 244/290 行及引擎口径一致）；补 `_compute_status` 单测：XD 日涨停应标 `LIMIT_UP`。全库 grep `== "交易"` 仅此 2 处，改完即清零。

### P1 —— 高：影响回测保真 / 求解稳定

**2. 停牌持仓被强制 `w=0`（卖出意图口径），虚占换手预算 + 假性资金释放**（= 待办2，未修复；[index_enhance.py:179](portfolio_optimizer/optimizer/index_enhance.py:179)、219-233；[alpha_max.py:199](portfolio_optimizer/optimizer/alpha_max.py:199)、239-253）

停牌股进 `banned_idx` 后约束 `w == 0`，但 `delta_w = |w - w_prev|` 对全体计算：持仓 3% 的票停牌即白占 3% 的 `max_turnover` 配额；同时 `sum(w)=1` 令优化器把停牌冻结资金"释放"再分配，实盘不可执行 → 系统性超配其他票。
**修复方向（业界冻结口径，动手前先定方案）**：停牌且 `w_prev>0` → 约束 `w == w_prev` 且不进 `delta_w`；停牌且 `w_prev=0` → `w == 0`。涉及两个优化器 + design.md/method.md 同步。

**3. prev_weight 掉池漂移：上期持仓掉出候选池即被 reindex 抹掉**（= 待办3，未修复；[batch_optimize.py:235](portfolio_optimizer/pipeline/batch_optimize.py:235)-239，失败回退分支 298-305 同样）

`.reindex(snapshot.tickers).fillna(0).renorm` 把跌出 TOP_N / 转 ST / 停牌 / 退市的持仓"凭空清仓且不计换手"：换手约束与成本失真，业绩略被高估（漏付被动清仓成本）。指数增强宽候选池下掉池是常态，每期都在发生。
**修复方向**：优化域取 **候选池 ∪ 上期持仓**，掉池票设只卖不买（加仓上界 0），卖出计入换手与成本；`filter_universe` 需支持并集入参。与 #2 耦合（都动优化域与换手口径），建议一起设计。

**4. alpha_max 无 SCS 兜底、无 max_iter**（= 待办4a，未修复；[alpha_max.py:271](portfolio_optimizer/optimizer/alpha_max.py:271)-278）

仅 `prob.solve(solver=cp.CLARABEL)`，异常直接返回 infeasible（不重试），批处理层随即走失败回退分支（该分支同样带 #3 的抹平问题）。对比 [index_enhance.py:259](portfolio_optimizer/optimizer/index_enhance.py:259)-271 有完整 `max_iter=500 + SCS 降级`。~5000 只候选池下失败概率更高，两优化器稳定性不对称。
**修复**：与 index_enhance 对齐，建议抽公共求解函数。

### P2 —— 中

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| 5 | [engine.py:354](portfolio_optimizer/backtest/engine.py:354) | `cash = max(cash, 0.0)` 静默截断负现金（= 待办4c，未修复）：买入超支时 NAV 虚高且日志无感知 | 截断前 `if cash < -1.0: warnings.warn(...)`，报出日期与量级 |
| 6 | [report.py:37](portfolio_optimizer/backtest/report.py:37) | 年度分解表 Sharpe 未扣 rf_daily，而全期 Sharpe（[engine.py:93](portfolio_optimizer/backtest/engine.py:93)-94）已扣：同一份报告两种 Sharpe 定义，年度行系统性偏高约 0.08（rf=2%、日波动1.5%时） | `_annual_metrics` 加 `risk_free` 参数，与 `calc_metrics` 同式 |
| 7 | [engine.py:102](portfolio_optimizer/backtest/engine.py:102)、[report.py:308](portfolio_optimizer/backtest/report.py:308)/332/391 | 超额收益三处口径混用：全期=算术年化、年度行=几何、月度表=算术差。年超额>10%时差几十bp | 统一为几何超额（或统一算术并在文档显式声明） |
| 8 | [cne6_risk.py:97](portfolio_optimizer/risk/cne6_risk.py:97) | 因子协方差 F 仅对称化；`cp.psd_wrap` 只是"信任声明"不做检验，微小负特征值会让凸性失效、solver 难诊断地失败 | `F += 1e-8 * I` 或加载后断言 `eigvalsh(F).min() > -tol` |
| 9 | tests/test_optimizers.py | `active_weight_upper`（commit 49bd21e 新功能）零测试 | 断言 `max|w - w_bm| ≤ delta + tol` |
| 10 | [benchmark.py:297](portfolio_optimizer/data/benchmark.py:297)-302 | 重构路径整表 `.ffill()`：掉出指数的成分以陈旧市值权重残留在基准里（非前视，默认 official 路径不受影响） | ffill 限制在"在册区间"内：按 `is_zzXXX==1` 掩码，出册后置 NaN 再归一 |
| 11 | [cli.py:124](portfolio_optimizer/cli.py:124)-133 | `backtest` 子命令缺 `--risk-free`（底层 run.py:63 支持），CLI 直跑无法调 Sharpe 无风险利率 | 补参数，默认 0.02 |

### P3 —— 低 / 整洁

| # | 位置 | 问题 |
|---|---|---|
| 12 | 两优化器 risk 分支 else 路径 | `risk_aversion=0.0` 且**未传** `risk_snapshot` 时，L2 分散惩罚意外生效（e084cf2 只修了"有 snapshot"场景）。加 `elif risk_aversion == 0.0: penalty = 0` 或文档声明 |
| 13 | [index_enhance.py:172](portfolio_optimizer/optimizer/index_enhance.py:172) vs 247、184 vs 219 | `delta` 一名两义（主动偏离上限 / 特质方差）、`w_prev` 重复定义。重命名 `active_ub`、提升 `w_prev` |
| 14 | [benchmark.py:248](portfolio_optimizer/data/benchmark.py:248)；[engine.py:275](portfolio_optimizer/backtest/engine.py:275) | trade_status 列加载后从未使用（无效 I/O）；`pending_rebal in weight_df.index` 恒真条件 |
| 15 | [real_adapter.py:247](portfolio_optimizer/data/real_adapter.py:247)-252 | `_compute_adv` 逐票 `groupby.apply`（= 待办4d，未修复）。改 polars `over("code") + rolling_mean` 预计算宽表，纯提速无口径变化 |
| 16 | [universe.py:166](portfolio_optimizer/pipeline/universe.py:166) | `build_synthetic_alpha` 前视警告仅 docstring 一句，弱于 signal_grid 的显式 ⚠️；报告/输出加水印防呆（待办4"水印"项）也未落实 |
| 17 | 文档 | 首日组合收益固定为 0（全仓现金等 T+1 建仓）与基准不对等，行为未在 method.md 声明 |
| 18 | docs/method.md §4 | `weight_upper` 写"两策略一致 0.01"，但 Python 类默认 alpha_max=0.02 / index_enhance=0.05（YAML 才是 0.01）。应注明 §4 数值来自 YAML |
| 19 | [design.md:133](docs/design.md:133)-134 | 幽灵条目 `factors/alpha_factors.py`（= 待办4b，未修复），该文件从未存在，删掉或改指真实位置 |
| 20 | configs/ | 两个 DLF 配置（`index_enhance_dlf_gru/mlp.yaml`）未 git 跟踪、未在操作指南登记；参数合理、alpha 文件存在、schema 兼容，建议 `git add` + 文档登记 + 注明"alpha 来自 DLF test 期（2024-01 起），非合成前视信号" |
| 21 | ~/.claude/commands/gaojn_skill.md:106（项目外） | 仍引用旧名 `backtest/realistic_engine.py`，实为 `engine.py`（= 待办5，未修复） |

### 测试覆盖缺口（专列）

- **real_adapter.py（313 行）零测试** —— 本次 P0 红线正是漏测才存活；`_compute_status`、`build_snapshot_from_panel` 是最该优先补的两处。
- **batch_optimize `run_batch_optimize` 端到端零测试** —— prev_weight 漂移（#3）所在路径。
- engine：XD/XR/N 日不被误拦、停牌阻断**卖出**（现只测了买入）、cash 不足缩量买入、pending 卖单跨日级联后最终成交、新调仓清空旧 pending。
- 优化器：换手约束、涨跌停约束（涨停 `w≤w_prev`/跌停 `w≥w_prev`）、SCS 兜底触发路径（mock CLARABEL 失败）。
- `signal_grid.py`（308 行）、`analysis/run.py` 零测试；test_report 只验 HTML 关键字不验数值。

### 数据侧一次性核验（非代码缺陷，静态审查无法证实）

`is_hs300/zz500/zz1000/is_st` 是否真 point-in-time 取决于 ClickHouse 视图 `vw_ashare_daily_backtest` 的定义。建议抽一只已知调入/调出 zz500 的票，确认标记随调仓日切换而非用当前成分回填全历史。外部 alpha 文件的 date 语义（"该日收盘可知"）同属跨仓交接口径，信任上游。

---

## 三、优化建议（方案级，非缺陷）

1. **停牌冻结口径 + 掉池并集域一起设计**（#2 + #3）：二者都改"优化域与换手的定义"，一次讨论定案、一次改造落地，避免两遍动 `batch_optimize` / 两个优化器 / 文档。落地顺序：先 `filter_universe` 支持并集与"只卖不买"标记 → 优化器加冻结约束与 `delta_w` 掩码 → method.md/design.md 同步。
2. **求解兜底抽公共函数**：`solve_with_fallback(prob, max_iter=500, fallback=SCS)`，两优化器共用，消除行为不对称（#4），并顺手让兜底路径可测。
3. **绩效口径统一**：超额收益统一几何（engine `calc_metrics` 改几何年化），Sharpe 的 rf 贯通年度表（#6/#7 一次完成），文档注明口径。
4. **补测优先级**：`_compute_status`（XD 涨停）> `active_weight_upper` > batch_optimize 端到端小样本 > 引擎 XD/XR/N 与卖出阻断 > SCS 兜底。
5. **性能**：ADV 预计算宽表（#15）；属纯提速，放最后。
6. **文档批处理**：#17/18/19/20/21 一次提交清完。

---

## 四、记忆待办核对（今晨清单 → 本轮复核）

| 今晨待办 | 本轮状态 | 本轮定级 |
|---|---|---|
| 1. real_adapter `ts=="交易"` 红线 | **未修复** | P0（#1） |
| 2. 停牌冻结口径 | **未修复** | P1（#2） |
| 3. prev_weight 漂移 | **未修复** | P1（#3） |
| 4a. alpha_max SCS 兜底 | **未修复** | P1（#4） |
| 4b. design.md alpha_factors.py | **未修复** | P3（#19） |
| 4c. cash 截断告警 | **未修复** | P2（#5） |
| 4d. _compute_adv 性能 | **未修复** | P3（#15） |
| 4e. 合成 alpha 水印防呆 | **未落实** | P3（#16） |
| 5. gaojn_skill 旧文件名 | **未修复** | P3（#21，项目外） |

8 项全部未动；期间提交（归因模块等）均为新功能。

---

## 五、建议修复批次

- **批次 1 —— 局部小改，可立即执行**：#1（红线+单测）、#4（SCS 兜底）、#5（cash 告警）、#6（年度 Sharpe rf）、#11（CLI 参数）、#19/20/21（文档与配置登记）。每项 ≤2 文件，互相独立。
- **批次 2 —— 口径改造，先讨论方案再动手**：#2 停牌冻结 + #3 掉池并集域（耦合，一起设计；涉及优化器×2 + pipeline + 文档，按房规先拆任务）。
- **批次 3 —— 陆续消化**：#7 超额口径统一、#8 PSD、#10 benchmark ffill 掩码、#12/13/14 整洁项、测试补充清单、#15 性能。

---

*4 份子审查中间报告已按惯例清理；本文件为唯一保留的最终报告。*
